# OpenWallet MCP

## Local repo layout

Three sibling repos share the same parent folder:

| Repo | Relative path from this repo |
|------|------------------------------|
| web | `../web` |
| api | `../api` |
| mcp (this) | `.` |

When asked to "edit local web repo" → work in `../web`. "edit local api repo" → work in `../api`.
