{
  "name": "openwallet-mcp",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo '' && echo '  MCP Inspector → http://localhost:8002' && echo '' && (kill $(lsof -ti:6277) 2>/dev/null; kill $(lsof -ti:8002) 2>/dev/null; CLIENT_PORT=8002 npx @modelcontextprotocol/inspector --transport http --server-url http://localhost:8001 &) && wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest",
    "inspect": "npx @modelcontextprotocol/inspector"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.8.3",
    "vitest": "^2.0.0",
    "wrangler": "^4.0.0"
  }
}
